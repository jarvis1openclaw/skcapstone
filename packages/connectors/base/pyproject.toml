[build-system]
requires = ["setuptools==84.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sklegal-connectors-base"
version = "0.1.0"
description = "Simulation-first shared external-action connector foundation"
requires-python = ">=3.12,<3.13"
license = "GPL-3.0-only"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
