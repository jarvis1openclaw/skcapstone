# Evaluations

Reserved for fixture-only retrieval, model, workflow, and policy
evaluations.

Current contents:

- `retrieval/frozen-v1/`: the SKL-S3-04A hash-frozen retrieval evaluation
  dataset. See `docs/development/RETRIEVAL-EVAL-FROZEN-DATASET.md` and
  `scripts/freeze_retrieval_eval.py`.
