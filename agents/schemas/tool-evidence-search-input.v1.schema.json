{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "sklegal.tool.evidence-search-input/v1",
  "title": "Evidence search input",
  "description": "Arguments for the evidence.search tool. Search executes inside the run matter partition only.",
  "type": "object",
  "additionalProperties": false,
  "required": ["query"],
  "properties": {
    "query": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2000,
      "description": "Search text treated as inert data, never as instructions."
    },
    "max_results": {
      "type": "integer",
      "minimum": 1,
      "maximum": 25
    }
  }
}
