# Screenshots

Drop `login.png` here — a screenshot of the Nextcloud login page showing the
**"Sign in with CapAuth"** button (and ideally the PGP challenge step).

`appinfo/info.xml` references it at:

    https://raw.githubusercontent.com/smilintux/capauth/main/src/capauth/integrations/nextcloud/screenshots/login.png

Requirements (App Store): served over `https://`, ≤ 2 MiB. PNG or JPG.
A release with a 404 screenshot URL is rejected.
